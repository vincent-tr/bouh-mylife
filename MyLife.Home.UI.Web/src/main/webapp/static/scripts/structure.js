/**
 * Gestion d'obtention des données de structure
 */

angular.module('mylife.structure', [], function($provide) {
	$provide.factory('$structure', ['$log', '$cacheFactory', function($log, $cacheFactory) {
		// TODO
	}]);
});