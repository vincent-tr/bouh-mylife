package mylife.home.irc.server.commands;

public class User {
	/*
	Server me = ce.getServer().getNetwork().findLocalServer();
	Message message = new Message(null, "NICK", nick, "0",
			user.getUser(), user.getHost(), String.valueOf(me.getToken()),
			user.getModes().toString(), user.getRealName());
	*/

}
